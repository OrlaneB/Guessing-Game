const guessingGame = require("../src/app");
// const { isClass } = require("./utilities");

describe('the guessedNumber',() => {
    test('should be a number', () => {
        expect(typeof guessedNumber).toBe('number');
    });
});
